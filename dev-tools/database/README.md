# Database

## To deploy in local:
```
cd ./database
docker-compose up [-d]
```
you need to set up a postgres user, password and DB in the envirronement variable.

defaults are admin, admin, ochocast-db.

default port is 5432:5432.

default port is ./postgres-data.


## To deploy in public:
Same process as in local.
