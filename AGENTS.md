# Agent instructions

## Skills

Reusable procedures live in `.agents/skills/<name>/SKILL.md` (open Agent Skills format). If your tool does not load skills automatically, read the file and follow it.

- `fix-deps`: resolve Dependabot alerts and dependency updates across the monorepo, fix breakages, open a PR. See `.agents/skills/fix-deps/SKILL.md`.
